#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    long long int T;
    cin >> T;
    int a, b;
    for(long long int i = 0; i < T; i++){
        cin >> a >> b;
        cout << a+b << "\n";
    }
}